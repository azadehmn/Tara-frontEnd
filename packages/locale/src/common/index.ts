import type { LocaleMessages } from '../types';
import { commonEn } from './en';
import { commonFa } from './fa';

/** Strings reused by every Tara app (buttons, generic errors). */
export const commonMessages: LocaleMessages = {
  fa: commonFa,
  en: commonEn,
};
